{
  "name": "order-manager",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev:gateway & npm run dev:components",
    "dev:gateway": "npm run dev --workspace=gateway",
    "dev:components": "echo 'Components not ready yet'",
    "setup": "bash scripts/setup.sh",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces"
  },
  "workspaces": [
    "gateway",
    "frontend/components",
    "frontend/dashboard",
    "shared/types"
  ],
  "devDependencies": {
    "@types/node": "^24.7.0",
    "@types/react": "^19.2.2",
    "concurrently": "^8.2.2",
    "tsx": "^4.6.2",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=18.19.0",
    "npm": ">=10.0.0"
  },
  "dependencies": {
    "axios": "^1.12.2",
    "react": "^19.2.0"
  }
}
